; Настройка терминала.
;
; Может использоваться как общий так и как персональный файл
; конфигурации. Параметры заданные в персональном файле перекрывают
; заданные в общем.
;
; Некоторые параметры являются ссылками на другие файлы. Если имя ссылки
; начинается с "~/", то файл ищется в персональном каталоге конфигурации
; терминала. В противном случае - в общем (корневом) каталоге
; конфигурации.
;
; Может содержать  секции  [host], [connections], [printing], [printers],
; [sound], [screen], [power] и [security].

; Основные параметры настройки терминала.

[host]

; Параметры:
;
;  hostname - наименование терминала.
;    Значение по умолчанию - tc-default.
;
;    Используется для указания имени клиента при подключении к
;    терминальным серверам.
;
;    При отсутствии параметра name в файле network используется для
;    регистрации имени терминала в DNS.
;
;    Использование этого параметра в общем файле конфигурации не
;    рекомендуется.

hostname = t75-NNNNNNN

;  domain - имя домена DNS для регистрации имени терминала.
;    Значение по умолчанию отсутствует.
;
;    Параметр  может быть указан в другом файле конфигурации терминала -
;    system.  Параметр указанный в файле network имеет преимущество перед
;    указанным в файле system.
;
;    Основным местом настройки параметра является указание его в  system.
;    Персональный файл network может использоваться для настройки
;    нескольких сетевых интерфейсов у терминала (если такие вдруг
;    найдутся). При этом каждый персональный файл network используется
;    для настройки соответствующего сетевого интерфейса.
;
;    Использование этого параметра в общем файле конфигурации не
;    рекомендуется.

;domain = chel.cbr.ru

;  localtime - ссылка на файл с описанием временной зоны для терминала.
;    Значение по умолчанию отсутствует.
;
;    Параметр ссылается на файл формата zoneinfo (см. zic(8), zdump(1)).

;localtime = localtime

;  remote-syslog - имя сервера, выполняющего сбор журналов событий
;  терминалов.
;    Значение по умолчанию отсутствует.

;  utc - признак использования в BIOS Всемтрного времени (UTC).
;    возможные варианты:
;      on  - используется Всемтрное время (UTC);
;      off - используется местное время.
;    Значение по умолчанию - no.
;
;utc = off


;remote-syslog = s75ocs01

;  logins - список пользователей, отображаемых в окне регистрации для
;           быстрого выбора. Разделитель списка — запятая.
;    Значение по умолчанию отсутствует.
;
;    RDP клиент имеет ограничение на длину имени пользователя и длину
;    пароля: имя - 20 символов, пароль - 12. Поэтому для RDP подключений
;    рекомендуется использовать NetBIOS формат имени: DOMAIN\User.

logins = REGION\75UserIO, user@CHEL.CBR.RU

;  autologon - признак автоматического подключения после загрузки.
;    возможные варианты:
;      on  - автоматическое подключение включено;
;      off - автоматическое подключение отключено.
;    Значение по умолчанию - off (отключено).
;
;    Как правило параметр используется совместно с СЗИ от НСД Аккорд.

;autologon = off

;  debug - признак отладки.
;    возможные варианты:
;      on  - отладки включена;
;      off - отладка отключена.
;    Значение по умолчанию - on (включено).
;
;    При  включенной отладке становится доступным запуск терминала для
;    проведения отладочных работ.

;debug = on

;  ntp-dhcp - признак получения информации о NTP-серверах через DHCP.
;    возможные варианты:
;      on  - получать список серверов;
;      off - не получать список серверов.
;    Значение по умолчанию - on (получать).

;ntp-dhcp = on

;  ntp-server - список NTP серверов.
;    Значение по умолчанию отсутствует.

;ntp-server = 75ntp

;  rdate-server - список rdate-серверов (RFC 868).
;    Значение по умолчанию отсутствует.

;rdate-server =

;  snmp - признак включения SNMP.
;    возможные варианты:
;      on  - SNMP включен;
;      off - SNMP отключен.
;    Значение по умолчанию - off (выключен).
;
;snmp = off

;  snmp-community - группа SNMP.
;    Значение по умолчанию - public.

;snmp-community = public

;  location - расположение терминала. Используется для настройки SNMP.
;    Значение по умолчанию - empty.

;location = v402

;  contact - контактная информация. Обычно почтовый адрес ответственного
;            лица. Используется для настройки SNMP.
;    Значение по умолчанию - empty.

;contact = NabiullinaES@cbr.ru


; Параметры настройки подключений.
;
; RDP.
;
; Файл подключений RDP rdp-apps имеет формат сходный с appsrv.ini. Он
; содержит секцию [ApplicationServers], каждый параметр которой
; соответствует подключению RDP Каждому подключению указанному в
; [ApplicationServers] соответствует отдельная секция с соответствующим
; именем содержащая параметры подключения.
;
; Параметры подключения:
;   Address - IP-адрес или DNS-имя терминального сервера.
;
; Дополнительные параметры.
;   Файлы подключений  rdp-apps и ica-apps кроме стандартных могут
;   содержать дополнительные параметры предназначенные для управления
;   пользовательским интерфейсом терминала. Имена таких параметром
;   начинаются с TC_.
;
;   Параметры:
;     CDMAllowed     - возможность проброса сменных носителей. По
;     		       умолчанию включено;
;     UsePrintcap    - возможность проброса принтеров. По умолчанию
;     		       включено;
;     ClientAudio    - возможность проброса звука. По умолчанию включено.
;     TC_Name        - имя подключения отображаемое в интерфейсе
;      		       терминала;
;     TC_Index       - порядковый номер подключения - для сортировки;
;     TC_DisableCred - если установлен в On - недоступен ввод имени
;     		       пользователя и пароля. Основное назначение -
;		       совместная работа с СЗИ от НСД.

;[connections]

; Параметры:
;
;  ica-apps - ссылка на файл подключений Citrix.
;    Значение по умолчанию отсутствует.
;
;    Указанный файл используется клиентом Citrix в качестве appsrv.ini.
;    Пустой параметр или его отсутствие отключает использование клиента
;    Citrix.

;ica-apps =

;  ica-client - ссылка на файл настройки клиента Citrix.
;    Значение по умолчанию отсутствует.
;
;    Указанный файл используется клиентом Citrix в качестве wfclient.ini.

;ica-client =

;  rdp-apps - ссылка на файл подключений RDP.
;    Значение по умолчанию отсутствует.
;
;    Указанный файл содержит описание подключений для клиента RDP. Пустой
;    параметр или его отсутствие отключает использование клиента RDP.

;rdp-apps =

;  app-index - порядковый номер подключения используемого по умолчанию.
;    Значение по умолчанию - 0.
;
;    На основе параметров ica-apps и rdp-apps создаётся список
;    подключений. Используя app-index, можно указать какое подключение
;    будет использоваться по умолчанию, а так же для автоматического
;    подключения после загрузки терминала (autologon = on).

;app-index = 0

; Параметры подсистемы печати.

;[printing]

; Параметры:
;
;  default - принтер по умолчанию.
;    Значение по умолчанию отсутствует.
;
;    Принтер из списка параметров раздела [printers].

;default =

;  drivers - ссылка на файл со списком драйверов принтеров.
;    Значение по умолчанию отсутствует.
;
;    Файл содержит список соответствия идентификатора принтера и его
;    драйвера для Windows.
;
;    Файл содержит одну секцию [printers]. Параметры в секции
;    идентификаторы принтеров, значения - названия драйверов для Windows.

;drivers =

; Подключенные принтера.

;[printers]

; Параметры:
;
;  Каждый параметр в секции - имя принтера. Каждому принтеру
;  соответствует отдельная секция с описанием принтера.
;
;  Основное назначение секции - подключение локальных принтеров для
;  которых не работает автоподключение и сетевых принтеров для которых
;  нет очереди печати.
;
;  Автоподключение не работает для LPT-принтеров. Для корректного
;  подключения необходимо в соответствующей секции указать устройство и
;  драйвер принтера. Для LPT-принтера как правило используется устройство
;  /dev/lp0.

;printer-local   =
;printer-network =

; Описание локального принтера с именем "printer-local".

;[printer-local]

; Параметры:
;  type - тип подключенного принтера.
;    возможные варианты:
;      local   - локальный принтер подключенный к терминалу;
;      network - сетевой принтер.
;    Значение по умолчанию отсутствует.

;type = local

;  device - устройство принтера.
;    Значение по умолчанию отсутствует.

;device = /dev/lp0

; driver - имя драйвера для Windows.
;    Значение по умолчанию отсутствует.

;driver =

; Описание сетевого принтера с именем "printer-network".

;[printer-network]

; Параметры:
;  type - тип подключенного принтера.
;    возможные варианты:
;      local   - локальный принтер подключенный к терминалу;
;      network - сетевой принтер.
;    Значение по умолчанию отсутствует.

;type = network

;  device - DNS-имя или адрес принтера.
;    Значение по умолчанию отсутствует.

;device =

;  port - номер порта принтера.
;    Значение по умолчанию отсутствует.
;
;    Обычно сетевого принтера используется порт 9100.

;port = 9100

; driver - имя драйвера для Windows.
;    Значение по умолчанию отсутствует.

;driver =

; Настройка звука.

;[sound]

; Параметры:
;
;  Каждый параметр в секции - имя элемента управления настройками звука.
;  Список элементов управления можно получить командой: amixer contents.
;  Значения параметров - параметры настройки элемента управления, эти
;  параметры передаются команде amixer sset (см. man amixer).
;
;  Количество и имена элементов управления сильно отличаются в
;  зависимости от аппаратного обеспечения терминала. В общий файл
;  конфигурации внесены наиболее часто встречающиеся. Для точной
;  настройки звука может потребоваться дополнительное указание параметров
;  в индивидуальном файле.

;Master  = 100% unmute
;Speaker = 0% mute

; Настройка экрана и интерфейса терминала.

;[screen]

; Параметры:
;  size - разрешение экрана.
;    Значение по умолчанию отсутствует.
;
;    Значение параметра передаётся команде xrandr -s (см. man xrandr).

;size = 800x600

;  output - разрешение экрана.
;    Значение по умолчанию отсутствует.
;
;    Имя параметра представляет собой имя экрана, значение - пара
;    значений. 1 значение - разрешение экрана, 2 - частота обновления
;    экрана (см. man xrandr). Оба значения передаются команде
;    xrandr --output [output1] --mode [mode] --rate [rate].
;    Значение частоты обновления является не обязательным.

;output1 = mode1 [rate1]
;output2 = mode2 [rate2]

;  xresources - ссылка на файл настройки интерфейса терминала.
;    Значение по умолчанию отсутствует.
;
;    Файл настройки интерфейса терминала представляет собой базу ресурсов
;    XRDB (см. man xrdp).

;xresources =

;  wallpaper - ссылка на файл обоев рабочего стола терминала.
;    Значение по умолчанию отсутствует.
;
;    Файл обоев рабочего стола представляет собой графический файл
;    формата PPM/PGM (см. man 3tk photo).

;wallpaper =

; Управление питанием.

;[power]

; Параметры:
;   dpms - управление питанием экрана.
;    Значение по умолчанию отсутствует.
;
;   Значение параметра - от одной до трех числовых величин - временные
;   интервалы для перевода экрана в состояния  standby, suspend и off.
;   Величины передаются команде xset dpms (см. man xset).

;dpms = 10 20 39

;  screensaver - управление настройками хранителя экрана.
;    Значение по умолчанию отсутствует.
;
;   Значение параметра - от одной до двух числовых величин - временные
;   интервалы для включения и замены хранителя экрана. Величины
;   передаются команде xset s (см. man xset).

;screensaver = 300 300

; Управление подключаемыми устройствами.

;[security]

; Параметры:
;   whitelist - ссылка на файл управления подключаемыми устройствами.
;    Значение по умолчанию отсутствует.
;
;    На основе параметров этого файла генерируются правила для системы
;    udev. Если файл не указан, то для пользователя будут доступны
;    принтера и недоступны сменные носители.

;whitelist =
