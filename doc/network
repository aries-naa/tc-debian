; Настройка сетевого интерфейса.
; 
; Может использоваться как общий так и как персональный файл 
; конфигурации. Параметры заданные в персональном файле перекрывают
; заданные в общем.
;
; Содержит одну секцию [network].

;[network]

; Параметры:
;
;  interface - команда используемая для настройки интерфейса.
;    возможные варианты:
;      none    - не настраивать сетевой интерфейс;
;      ethtool - использовать ethtool;
;      mii     - использовать mii-tool.
;      Значение по умолчанию - none.
;
;    Как правило используется ethtool и только в особых mii-tool. В
;    некоторых случаях, например на компьютерах Aquarius, настройку лучше
;    не делать во избежание зависания интерфейса.

;interface = ethtool

;  speed - скорость передачи.
;    возможные варианты:
;      100 - 100 Mb;
;      10  - 10 Mb.
;      Значение по умолчанию отсутствует.
;
;    Если значение не задано, то настройка с помощью  mii выполнятся не
;    будет, а настройка с помощью ethtool будет выполнена без указания
;    скорости.

;speed = 100

;  duplex - режим дуплекса.
;    возможные варианты:
;      full - полный дуплекс:
;      half - половинный дуплекс;
;      Значение по умолчанию отсутствует.
;
;    Если значение не задано, то настройка с помощью  mii выполнятся не
;    будет, а настройка с помощью  ethtool  будет выполнена без указания
;    режима дуплекса.

;duplex = full

;  autoneg - автоматическая настройка интерфейса.
;    возможные варианты:
;    on  - включена;
;    off - выключена.
;      Значение по умолчанию отсутствует.
;
;    Если значение не задано, то настройка с помощью  mii  выполнятся не
;    будет, а настройка с помощью  ethtool  будет выполнена без указания
;    параметра автоматической настройки.

;autoneg = off

;  domain - имя домена DNS для регистрации имени терминала.
;    Значение по умолчанию отсутствует.
;
;    Параметр  может быть указан в другом файле конфигурации терминала -
;    system. Параметр указанный в файле network имеет преимущество перед
;    указанным в файле system.
;
;    Основным местом настройки параметра является указание его в system.
;    Персональный файл network может использоваться для настройки
;    нескольких сетевых интерфейсов у терминала (если такие вдруг
;    найдутся). При этом каждый персональный файл network используется
;    для настройки соответствующего сетевого интерфейса.
;
;    Использование этого параметра в общем файле конфигурации не
;    рекомендуется.

;domain = chel.cbr.ru

;  name - имя сетевого интерфейса для регистрации в DNS.
;    Значение по умолчанию отсутствует.
;
;    Параметр может быть указан в другом файле конфигурации терминала -
;    system. Параметр указанный в файле network имеет преимущество перед
;    указанным в файле system.
;
;    Основным местом настройки параметра является указание его в  system.
;    Персональный файл network может использоваться для настройки
;    нескольких сетевых интерфейсов у терминала (если такие вдруг
;    найдутся). При этом каждый персональный файл network используется
;    для настройки соответствующего сетевого интерфейса.
;
;    Использование этого параметра в общем файле конфигурации не
;    рекомендуется.

;name = t75-NNNNNNN
